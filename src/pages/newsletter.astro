---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title="Dev Dispatch Newsletter" description="Subscribe to Dev Dispatch newsletter for insights on software development, design, product management, and more. Bi-weekly updates with curated content and interesting finds." />
		<style>
			main {
				width: 960px;
				max-width: calc(100% - 2rem);
				margin: auto;
				padding: 3rem 1rem;
			}
			
			.newsletter-header {
				text-align: center;
				margin-bottom: 3rem;
			}
			
			.newsletter-header h1 {
				margin: 0 0 0.5rem 0;
				color: var(--text-primary);
				font-weight: var(--font-weight-bold);
				letter-spacing: var(--tracking-tight);
			}
			
			.newsletter-header p {
				margin: 0 0 2rem 0;
				color: var(--text-secondary);
				font-size: var(--text-sm);
			}
			
			.newsletter-cta {
				margin: 2rem 0;
			}
			
			.cta-button {
				display: inline-block;
				background: var(--accent);
				color: white;
				padding: 1rem 2rem;
				border-radius: var(--radius);
				text-decoration: none;
				font-weight: var(--font-weight-medium);
				font-size: var(--text-lg);
				transition: all var(--default-transition-duration) var(--default-transition-timing-function);
				border: 2px solid var(--accent);
				box-shadow: var(--shadow-md);
			}
			
			.cta-button:hover {
				background: var(--accent-dark);
				border-color: var(--accent-dark);
				transform: translateY(-2px);
				box-shadow: var(--shadow-lg);
			}
			
			.newsletter-content {
				max-width: 700px;
				margin: 0 auto;
			}
			
			.content-section {
				background: var(--background);
				padding: 2.5rem;
				border-radius: var(--radius-lg);
				box-shadow: var(--shadow-md);
				margin-bottom: 2rem;
				border: 1px solid var(--border);
			}
			
			.content-section h2 {
				color: var(--text-primary);
				margin: 0 0 1.5rem 0;
				font-size: var(--text-2xl);
				font-weight: var(--font-weight-medium);
			}
			
			.content-section p {
				color: var(--text-secondary);
				line-height: var(--text-base--line-height);
				margin-bottom: 1.2rem;
			}
			
			.content-section p:last-child {
				margin-bottom: 0;
			}
			
			.stats-section {
				background: linear-gradient(135deg, 
					oklch(from var(--accent) l c h / 0.15) 0%, 
					oklch(from var(--accent) l c h / 0.08) 50%,
					oklch(from var(--accent) l c h / 0.05) 100%);
				border: 1px solid oklch(from var(--accent) l c h / 0.2);
			}
			
			.stats-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
				gap: 2rem;
				margin-top: 1.5rem;
			}
			
			.stat-item {
				text-align: center;
				padding: 1.5rem;
				background: oklch(from var(--background) l c h / 0.8);
				border-radius: var(--radius-md);
				border: 1px solid oklch(from var(--accent) l c h / 0.1);
			}
			
			.stat-number {
				font-size: var(--text-3xl);
				font-weight: var(--font-weight-bold);
				color: var(--accent);
				margin: 0 0 0.5rem 0;
			}
			
			.stat-label {
				color: var(--text-secondary);
				font-size: var(--text-sm);
				font-weight: var(--font-weight-medium);
				margin: 0;
			}
			
			.sponsor-section {
				background: linear-gradient(135deg, 
					var(--color-amber-200) 0%, 
					oklch(from var(--color-amber-200) l c h / 0.5) 50%,
					oklch(from var(--color-amber-200) l c h / 0.2) 100%);
				border: 1px solid var(--color-amber-400);
			}
			
			.sponsor-benefits {
				background: oklch(from var(--background) l c h / 0.8);
				padding: 1.5rem;
				border-radius: var(--radius-md);
				margin: 1.5rem 0;
				border: 1px solid var(--color-amber-400);
			}
			
			.sponsor-benefits h3 {
				color: var(--text-primary);
				margin: 0 0 1rem 0;
				font-size: var(--text-xl);
			}
			
			.sponsor-benefits ul {
				margin: 0;
				padding-left: 1.5rem;
				color: var(--text-secondary);
			}
			
			.sponsor-benefits li {
				margin-bottom: 0.5rem;
				line-height: var(--text-base--line-height);
			}
			
			.email-link {
				color: var(--accent);
				text-decoration: none;
				font-weight: var(--font-weight-medium);
			}
			
			.email-link:hover {
				text-decoration: underline;
			}
			
			.newsletter-icon {
				font-size: var(--text-5xl);
				margin-bottom: 1rem;
			}
			
			@media (max-width: 768px) {
				.newsletter-header h1 {
					font-size: var(--text-3xl);
				}
				
				.content-section {
					padding: 2rem;
				}
				
				.stats-grid {
					grid-template-columns: repeat(2, 1fr);
					gap: 1rem;
				}
				
				.stat-item {
					padding: 1rem;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<!-- Header Section -->
			<section class="newsletter-header">
				<h1>Dev Dispatch</h1>
                <div class="newsletter-icon">📧</div>
				<p>
					Bi-weekly newsletter about software development, design, product management, and interesting finds from the internet.
				</p>
				<div class="newsletter-cta">
					<a href="https://buttondown.email/kasuken" target="_blank" rel="noopener" class="cta-button">
						Subscribe to Dev Dispatch →
					</a>
				</div>
			</section>

			<div class="newsletter-content">
				<!-- About the Newsletter -->
				<section class="content-section">
					<h2>About the Newsletter</h2>
					<p>
						I send out a newsletter every two weeks or so with things I've been working on, things I've been reading, and interesting things I've found on the internet.
					</p>
					<p>
						Content is usually about software development, design, and product management. Sometimes I write about other things like music, or life in general.
					</p>
					<p>
						I won't spam though, I promise. You can unsubscribe at any time. I also won't share your email with anyone else.
					</p>
				</section>

				<!-- Newsletter Statistics -->
				<section class="content-section stats-section">
					<h2>Newsletter Statistics</h2>
					<p>
						Dev Dispatch has grown to over 1,000 subscribers with excellent engagement rates that continue to grow every week.
					</p>
					<div class="stats-grid">
						<div class="stat-item">
							<div class="stat-number">1000+</div>
							<div class="stat-label">Subscribers</div>
						</div>
						<div class="stat-item">
							<div class="stat-number">65%</div>
							<div class="stat-label">Open Rate</div>
						</div>
						<div class="stat-item">
							<div class="stat-number">24%</div>
							<div class="stat-label">Click Rate</div>
						</div>
						<div class="stat-item">
							<div class="stat-number">Bi-weekly</div>
							<div class="stat-label">Frequency</div>
						</div>
					</div>
				</section>

				<!-- Sponsorship Information -->
				<section class="content-section sponsor-section">
					<h2>Sponsorship Opportunities</h2>
					<p>
						I'm currently looking for sponsors for my newsletter. If you're interested in sponsoring, please send me an email!
					</p>
					
					<div class="sponsor-benefits">
						<h3>What You Get:</h3>
						<ul>
							<li>Short paragraph at the top of the newsletter with a link to your website</li>
							<li>Description of your product or service with a link at the bottom</li>
							<li>Permanent link from the newsletter archive page</li>
							<li>Exposure to 1000+ engaged subscribers</li>
							<li>High visibility with 65% open rate and 24% click rate</li>
						</ul>
					</div>
					
					<p>
						The format of the sponsorship is very easy and straightforward. The newsletters will be included in the archive, so you'll get a permanent link to your website from the archive page as well.
					</p>
					
					<p>
						If everything sounds good, please <a href="mailto:emanuele@bartolesi.com" class="email-link">send me an email</a> and we can discuss the details!
					</p>
				</section>

				<!-- Final CTA -->
				<section class="content-section">
					<h2>Ready to Join?</h2>
					<p>
						If you're interested in software development insights, curated content, and occasional thoughts on music and life, Dev Dispatch is for you!
					</p>
					<div class="newsletter-cta">
						<a href="https://buttondown.email/kasuken" target="_blank" rel="noopener" class="cta-button">
							Subscribe Now
						</a>
					</div>
				</section>
			</div>
		</main>
		<Footer />
	</body>
</html>
